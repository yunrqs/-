# 重构验证记录

- TypeScript 检查与 Vite 生产构建通过。
- 后端 HTTP 契约与眨眼单元测试共 18 项通过。
- 离线特征、数据清单和七种分类器训练冒烟测试共 5 项通过。
- 实际加载迁移后的情绪权重和眨眼检测器，以空白图像执行一次推理并关闭会话成功；未开启真实摄像头。
- 使用迁移后的现有走神模型完成 200 条特征预测，示例数据清单成功解析 3 个探针；验证输出已清理。
- 比对迁移前后源码，25 个未涉及路径调整的算法模块/会话方法 AST 一致，前端监测页面除导入外内容一致。
- Edge 浏览器首项交互测试通过；第二项首轮在虚拟摄像头初始化阶段超时，独立重跑交互断言通过，但浏览器进程退出持续等待，已手动停止。此次浏览器测试不视为整套无异常通过，未改动产品逻辑来规避该问题。

测试命令见 [开发文档](development.md)。没有重新训练或覆盖现有实验模型，也未验证真实摄像头画质或识别准确率。

## 2026-09-23 视线预测接入验证

- TypeScript 检查与 Vite 生产构建通过；本地 MediaPipe 文件随构建发布。
- 加权岭回归的已知坐标拟合、重置与无效特征测试通过（1 项）。
- 后端 HTTP/静态资源契约及眨眼测试全部通过（19 项）。
- Edge 浏览器测试全部通过（6 项，51.1 秒，退出码 0）：原有识别操作与
  迟到响应处理、本地真实 FaceMesh 加载及空白帧推理、16 点校准及独立误差
  验证、单次摄像头申请、丢脸/暂停/恢复/视口失效、模型加载失败与加载中关闭摄像头。
- 浏览器测试改用 Canvas 合成 MediaStream，避免本机虚拟摄像头驱动间歇性
  不返回画面；完整校准交互测试仅模拟人脸关键点，仍运行真实眼部图像特征与回归。
- 沙箱内浏览器退出持续等待；最终在获准的沙箱外运行中完成全部断言并正常退出。
- 已查看桌面与手机宽度截图，新增面板没有横向溢出。

尚未以真人摄像头评估个体注视误差；合成视频测试不代表真实预测精度。
未重新训练或修改原有情绪/走神模型。原 `视线预测` 目录保留作来源参考，
在线应用不加载其中的演示交互或鼠标监听入口。

## 2026-09-23 视线模块目录整理验证

- Hook 移入 gaze/hooks，服务移入 gaze/services，算法测试移入 tests/unit；仅调整导入路径，预测、校准和滤波逻辑未修改。
- `npm --prefix web-react run build` 通过 TypeScript 检查和 Vite 构建。
- `npm --prefix web-react run test:unit` 通过现有回归算法测试（1 项）。
- `npm --prefix web-react run test:e2e` 在获准的沙箱外运行中全部通过（6 项，51.3 秒，退出码 0）；沙箱内断言均通过，但 Edge 退出持续等待，已停止该次运行。
- 架构说明、开发命令、功能规格与 README 已同步新目录。真实摄像头预测精度不属于本次目录整理验证范围。
